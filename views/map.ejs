<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kizu Server Map</title>
    <style>
        html, body {
            margin: 0;
            padding: 0;
            height: 100%;
            width: 100%;
            overflow: hidden;
            font-family: Arial, sans-serif;
        }
        .map-container {
            width: 100%;
            height: 100vh;
            border: none;
        }
        .warning {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            background: #fff3cd;
            color: #856404;
            padding: 15px;
            text-align: center;
            z-index: 1000;
            display: none;
        }
        .warning a {
            color: #533f03;
            font-weight: bold;
        }
        .warning.show {
            display: block;
        }
    </style>
</head>
<body>
    <div id="warning" class="warning">
        This map requires HTTP access. If you're seeing this message, please visit 
        <a href="http://kizuserver.xyz/map" target="_blank">http://kizuserver.xyz/map</a> instead.
    </div>
    <iframe 
        id="mapFrame"
        src="http://play.kizuserver.xyz:25684/" 
        class="map-container"
        allowfullscreen>
    </iframe>

    <script>
        // Check if we're on HTTPS
        if (window.location.protocol === 'https:') {
            document.getElementById('warning').classList.add('show');
        }

        // Handle iframe load error
        document.getElementById('mapFrame').onerror = function() {
            document.getElementById('warning').classList.add('show');
        };
    </script>
</body>
</html> 